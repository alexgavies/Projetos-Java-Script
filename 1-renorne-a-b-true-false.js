//comparador de igualdade
        var x=2
        var y=2
       function verificar(a, b) {// a, b representa somente a a quantidade de parametros passado a função NÃO SÃO OS NOMES DA ARIAVEIS USADAS.

           return a===b //return sempre reponderá com valores boleanos true ou false, servindo de intermediarios para outras oerações.          
       }
       console.log(verificar(x, y))//valor de x ocupa o lugar de a, e valor de y ocupa o lugar de b.
